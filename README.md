🎓 Student Information Management System (Flask + MySQL + REST API)
📘 Project Overview

The Student Information Management System (SIMS) is a full-stack web application built using Python (Flask) and MySQL to manage student records efficiently.
It performs complete CRUD operations (Create, Read, Update, Delete), supports PDF export, and includes an extended REST API version for integration with external systems or mobile apps.

This project demonstrates Python backend development, database connectivity, and RESTful API design — perfect for learning or portfolio presentation.

🚀 Features
🖥️ Web Application

Add, edit, delete, and view student information

Store details like name, DOB, parents’ names, address, degree, stream, year of joining, etc.

Download specific or all student records as a PDF

Client-side search and filtering (real-time without page reload)

Client-side pagination (10 students per page)

Responsive Blue & White UI with smooth animations
